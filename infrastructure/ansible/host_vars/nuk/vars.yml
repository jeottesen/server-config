---

proxmox_api_user: "{{ vault_proxmox_api_user }}"
proxmox_api_password: "{{ primary_user_pass }}"
proxmox_api_host: "192.168.50.8"
proxmox_token_id: "{{ vault_proxmox_token_id }}"
proxmox_token_value: "{{ vault_proxmox_token_value }}"
