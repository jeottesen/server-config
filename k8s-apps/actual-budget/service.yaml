apiVersion: v1
kind: Service
metadata:
  name: actual-service
  namespace: actual-budget
spec:
  selector:
    app: actual-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5006
      nodePort: 30050
  type: NodePort