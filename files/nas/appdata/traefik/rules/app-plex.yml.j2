http:
  routers:
    plex-rtr:
      rule: "Host(`plex.{{ domain_name }}`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth
      service: plex-svc
  services:
    plex-svc:
      loadBalancer:
        servers:
          - url: "https://{{ plex_hostname }}:32400/"