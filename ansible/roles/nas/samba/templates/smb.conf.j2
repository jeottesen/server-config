{# Copyright (C) 2014-2019 Maciej Delmanowski <drybjed@gmail.com>
 # Copyright (C) 2015-2019 DebOps <https://debops.org/>
 # SPDX-License-Identifier: GPL-3.0-only
 #
 # Modified to remove unecessary features.
 #}
# {{ ansible_managed }}
{% macro print_hash(hash) %}
{% for key, value in hash | dictsort %}
  {# Convert True/False to yes/no, otherwise keep the value as-is #}
  {% set final_value = 'Yes' if value is sameas true else ('No' if value is sameas false else value) %}
  {{ "%-30s" | format(key | replace("_"," ")) }} = {{ final_value }}
{% endfor %}
{% endmacro %}
{% macro print_section(hash, section='global') %}
[{{ section }}]
{{ print_hash(hash) }}
{% endmacro %}

[global]
	# Default options
{{ print_hash(hash=samba__global) }}
{% if samba__global_custom is defined and samba__global_custom %}
	# Custom options
{{ print_hash(hash=samba__global_custom) }}
{% endif %}

{% if samba__shares is defined and samba__shares %}
{% for share in samba__shares.keys() %}
[{{ share }}]
{{ print_hash(hash=samba__shares[share]) }}
{% endfor %}
{% endif %}
